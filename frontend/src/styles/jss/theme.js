import teal from '@material-ui/core/colors/teal';

const theme = {
  palette: {
    primary: teal,
  },
  shape: {
    borderRadius: 0,
  },
  typography: {
    useNextVariants: true,
  },
};

export default theme;
